export function tutorialFlow(root,onDone){const steps=['Step 1: Tilt phone to steer the field.','Step 2: Tap the universe to emit a pulse.','Step 3: Shake device for a cosmic burst.','Step 4: Switch mode from the selector.'];let i=0;const draw=()=>{root.innerHTML=`<div class="panel"><strong>Tutorial</strong><p>${steps[i]}</p><button id='nextT' class='btn'>${i===steps.length-1?'Done':'Next'}</button><button id='skipT' class='btn'>Skip</button></div>`;root.querySelector('#nextT').onclick=()=>{i++;if(i>=steps.length){root.classList.add('hidden');onDone();}else draw();};root.querySelector('#skipT').onclick=()=>{root.classList.add('hidden');onDone();};};root.classList.remove('hidden');draw();}
